import { StyleSheet } from 'react-native';

export const CameraScanStyles = StyleSheet.create({

    container: {
        // flex: 1,
    },
    camera: {
        // flex: 1,
        height: '100%',

    }

});
